// export default function ChatContainer({ message }) {
//   message.map((msg, i) => {
//     console.log(msg.message);
//     return (
//       <span key={i} className={`${msg.fromSelf ? "sender" : "receiver"}`}>
//         {msg.message}
//       </span>
//     );
//   });
// }
